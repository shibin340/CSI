<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/csi_login"
    tools:context=".AdminAddEvent">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Add your events here!"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:textColor="#ffffff"
        android:layout_margin="15dp"
        android:textSize="25sp"
        android:id="@+id/addeve_tv" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/email_bg"
        android:hint="Event Title"
        android:layout_below="@id/addeve_tv"
        android:paddingLeft="10dp"
        android:layout_margin="10dp"
        android:id="@+id/eve_title"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/email_bg"
        android:hint="Event Description"
        android:layout_below="@id/eve_title"
        android:paddingLeft="10dp"
        android:layout_margin="10dp"
        android:id="@+id/eve_desc"
        />

    <TextView
        android:id="@+id/eve_date"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/eve_desc"
        android:layout_margin="10dp"
        android:background="@drawable/email_bg"
        android:gravity="center|start|left"
        android:paddingLeft="10dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:text="Date"
        android:textColor="@color/hintColor"
        android:textSize="18sp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/eve_bt"
        android:layout_below="@id/eve_date"
        android:textSize="18sp"
        android:layout_margin="15dp"
        android:background="@drawable/add_event_bt_bg"
        android:layout_centerHorizontal="true"
        android:text="Add Event"
        android:textColor="#000"
        android:padding="10dp"
        />
</RelativeLayout>